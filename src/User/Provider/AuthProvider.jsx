import { createContext } from "react";

export const UserContext = createContext();

const AuthProvider = () => {
    return (
        <div>
            
        </div>
    );
};

export default AuthProvider;